#!/bin/bash
j="0"
while test $j -eq "0"
do
	ping -c 3 google.com
	if test $? -eq "0"
		then 
		i="0"
		while test $i="0"
		do
			sudo xbps-install -y $*
			if test $? -eq "0"
				then
					break
					j=1
				fi
		done
		break
	fi
done
